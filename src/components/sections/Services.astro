---
import Service from "../cards/Service.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import Title from "../shared/Title.astro";
import { services } from "../../utils/data.ts";

type ServicesProps = {
    title: string;
    description: string;
    icon: string
};
---

<section id="services">
    <Container className="space-y-10 md:sapce-y-12">

        <div class="text-center max-w-3xl mx-auto space-y-4">
            
            <Title>
                Nuestra Oferta: Dos Pilares para el Crecimiento Digital
            </Title>
            
            <Paragraph>
                En <strong>BIESAL</strong>, resolvemos la necesidad digital de tu empresa con una <strong>solución integral de dos pilares</strong>. 
                Automatizamos tu gestión interna con nuestra plataforma (el cómo), y posicionamos tu marca en la web con sitios 
                que se conectan directamente a tu operación (el dónde).
            </Paragraph>
        </div>

        <div class="grid sm:grid-cols-2 lg:grid-cols-2 gap-6 lg:gap-8">
            {
                services.map((service: ServicesProps) => {
                    return (
                        <Service
                            title={service.title}
                            description={service.description}
                            icon={service.icon}
                        >
                        </Service>
                    );
                })
            }
        </div>

    </Container>
</section>