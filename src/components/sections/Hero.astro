---
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
---

<section class="relative pt-20 lg:pt-24">
    <!-- Finisher Header Background -->
    <div class="header finisher-header" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 0;"></div>

    <Container className={"flex flex-col relative z-10"}>
        
        <div class="absolute w-full lg:w-1/2 inset-y-0 lg:right-0 ">
            <span class="absolute -left-6 md:left-4 top-24 lg:top-28 w-24 h-24 rotate-90 skew-x-12 rounded-3xl bg-green-400 blur-xl opacity-60 lg:opacity-95 lg:block hidden"></span>
            <span class="absolute right-4 bottom-12 w-24 h-24 rounded-3xl bg-primary blur-xl opacity-80"></span>
        </div>
        <span class="w-4/12 lg:w-2/12 aspect-square bg-gradient-to-tr from-primary to-green-400 absolute -top-5 lg:left-0 rounded-full skew-y-12 blur-2xl opacity-40 skew-x-12 rotate-90"></span>

        <div class="relative flex flex-col items-center text-center lg:text-left 
        lg:items-start lg:max-w-none mx-auto lg:mx-0 lg:flex-1 w-full"> 
            
            <h1 class="text-3xl/tight sm:text-4xl/tight md:text-5xl/tight xl:text-6xl/tight
             font-bold text-heading-1">
             Transformamos tu visión digital en realidad, potenciado por <span class="text-transparent bg-clip-text bg-gradient-to-br from-indigo-600 from-20% via-primary via-30% to-green-600">BIESAL</span>
            </h1>

            <Paragraph className="mt-4"> Impulsamos a las empresas mediante la automatización de procesos administrativos y el 
                desarrollo de páginas web totalmente integradas con nuestro Sistema Administrativo. 
                Ayudamos a reducir tareas manuales, mejorar la eficiencia y centralizar la información 
                en una única plataforma conectada, moderna y segura.
            </Paragraph>

            
        </div>
        
    </Container>
</section>

<!-- Load Finisher Header Script -->
<script is:inline src="/scripts/finisher-header.es5.min.js"></script>

<script is:inline>
document.addEventListener('DOMContentLoaded', function() {
  // Initialize Finisher Header
  new FinisherHeader({
    "count": 30,
    "size": {
      "min": 2,
      "max": 6,
      "pulse": 0
    },
    "speed": {
      "x": {
        "min": 0,
        "max": 0.3
      },
      "y": {
        "min": 0,
        "max": 0.5
      }
    },
    "colors": {
      "background": "transparent",
      "particles": [
        "#4f46e5",
        "#10b981",
        "#3b82f6"
      ]
    },
    "blending": "overlay",
    "opacity": {
      "center": 0.8,
      "edge": 0
    },
    "skew": -1,
    "shapes": [
      "c"
    ]
  });
});
</script>

<style>
  /* Ensure finisher header canvas is behind content */
  .finisher-header {
    pointer-events: none;
  }
</style>